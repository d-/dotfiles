# Intentionally empty. The vi mode is conveyed by the colour of the prompt arrow
# in fish_prompt instead of fish's default [I]/[N] block. fish repaints the full
# prompt on mode change when this function is defined but produces no output.
function fish_mode_prompt
end
